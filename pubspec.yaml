name: walf
description: Walf is a secure and fast key-value based database based on JSON. It uses Salsa20 encryption algorithm for Walf security.
version: 1.0.2
homepage: https://github.com/zekkontro/walf-database

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.0.1
  logging: ^1.0.1
  path_provider_windows: ^2.0.1
  encrypt: ^5.0.0

  

dev_dependencies:
  flutter_test:
    sdk: flutter

